name: Run tests

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test upload action
        uses: appswithlove/updraft-deploy-github-action@969e2a2182318a1cb591db0b563a945d8b10b723
        with:
          api_key: ${{ secrets.TEST_API_KEY }}
          app_key: ${{ secrets.TEST_APP_KEY }}
          app_path: ./test/test.apk
      - name: Get the output
        run: echo "output ${{ steps.hello.outputs.response_json }}"
