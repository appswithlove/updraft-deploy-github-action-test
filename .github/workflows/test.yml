name: Run tests

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: ls -la
      - name: Test upload action
        uses: appswithlove/updraft-deploy-github-action@50e9999df1e4551a01623f34e7865552fdba1a8e
        with:
          api_key: ${{ secrets.TEST_API_KEY }}
          app_key: ${{ secrets.TEST_APP_KEY }}
          app_path: ${{ github.workspace }}/test/test.apk
      - name: Get the output
        run: echo "output ${{ steps.hello.outputs.response_json }}"
