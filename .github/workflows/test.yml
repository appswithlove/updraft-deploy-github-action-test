name: Run tests

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test upload action
        uses: appswithlove/updraft-deploy-github-action@ff97ece4064e4d17d39fc16ddc9b51fc09447707
        with:
          api_key: ${{ secrets.TEST_API_KEY }}
          app_key: ${{ secrets.TEST_APP_KEY }}
          app_path: ./test/test.apk
      - name: Get the output
        run: echo "output ${{ steps.hello.outputs.response_json }}"
